import React from "react";

const CouponScreen = () => {

  return (
    <div style={styles.container}>
      Coming Soon
    </div>
  );
}

const styles = {
  container: {
    display: "flex",
    justifyContent: "center",
    height: "100vh",
    alignItems: "center"
  }
}

export default CouponScreen;